{
  "name": "visitor-log",
  "private": true,
  "type": "module",
  "description": "QR visitor sign-in for a Newfoundland long-term care, personal care or group home. Local build; see README.md.",
  "scripts": {
    "demo": "node demo.mjs",
    "test:worker": "cd worker && npm test",
    "test:negative": "cd worker && npm run negative",
    "test:e2e": "cd app && npx playwright test",
    "test:e2e:negative": "cd app && npm run negative:visit && npm run negative:staff && npm run negative:journey"
  }
}
